function calculateStreakBonusXP(streak: number): number {
  return 0
}

function calculateDifficultyXP(difficulty: 'easy' | 'medium' | 'hard'): number {
  return 0
}

export { calculateStreakBonusXP, calculateDifficultyXP };